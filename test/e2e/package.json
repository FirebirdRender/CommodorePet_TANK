{
  "name": "tank-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "E2E tests for TANK! networked multiplayer game",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0"
  }
}